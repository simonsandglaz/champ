extends layout

block navigation
	nav(class='navbar container-fluid-nav fixed-top navbar-expand-lg navbar-dark bg-dark')
		div(class="container-fluid-nav text-center")
			a(class='navbar-brand back' href='/assessment')
				i(class="fas fa-arrow-left")
			a(class='navbar-brand' href='#')
				block question_category
					#{question.type.toUpperCase()}
	nav(class="navbar container-fluid-nav fixed-bottom navbar-expand-lg navbar-light bg-light comment-buttons question-buttons-nav")
		a(href=("/" + question.type + "/" + question.id) class="btn btn-primary btn-lg" role="button" aria-pressed="true") Save Comments

block content
	div(class="mb-3")
		label(for="exampleFormControlTextarea1" class="form-label") Comments:
		textarea(class="form-control" id="exampleFormControlTextarea1" rows="10")
		div(class="form-check")
			input(class="form-check-input" type="checkbox" value="" id="flexCheckDefault")
			label(class="form-check-label" for="flexCheckDefault") I would like to expand on this answer at my next appointment

	div(class='container-md menu question-buttons question-buttons-separate')
		a(href=("/" + question.type + "/" + question.id) class="btn btn-primary btn-lg" role="button" aria-pressed="true") Save Comments